# Mconf
the config files in my mac for backup.

`crontab -e`

`MAILTO=""`
`* * * * * sh /Users/rkd/github/MConf/auto-update.sh > /dev/null`

file at `/var/at/tabs`

to add file for eg:
`cd /path/to/MConf`
`ln ~/.vimrc .`
